#include <bits/stdc++.h>
using namespace std;

bool is_uniform(string& s) {
  char c = s[0];
  for (auto& e : s) {
    if (e != c) return false; 
  }
  return true;
}

int main() {
  int n; cin >> n;
  while (n--) {
    string s; cin >> s;
    while (!is_uniform(s)) {
      string       
    }
  }
}